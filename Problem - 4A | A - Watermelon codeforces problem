#include <stdio.h>
 
char* divide(int w) {
 
    if (w <= 2 || w % 2 != 0) {
        return "NO";
    } else {
        return "YES";
    }
}
 
int main() {
 
    int w;
    scanf("%d", &w);
 
 
    char *result = divide(w);
    printf("%s\n", result);
 
    return 0;
}
